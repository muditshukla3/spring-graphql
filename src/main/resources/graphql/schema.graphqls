type Book {
    id: ID!
    title: String
    pages: Int
    rating: Float
    author: Author
}

type Author {
    id: ID!
    firstName: String
    lastName: String
    books: [Book]
}

type Query{
    allBooks: [Book]
    findOne(id: ID!): Book
}
